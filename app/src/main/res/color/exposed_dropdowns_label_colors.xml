<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:alpha="0.4" android:color="@color/label_primary" android:state_enabled="false"/>

    <!-- Focused -->
    <item android:color="@color/label_primary" android:state_focused="true"/>

    <!-- Normal -->
    <item android:color="@color/label_primary"/>
</selector>